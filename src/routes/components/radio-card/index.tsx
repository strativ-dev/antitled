import { createFileRoute } from '@tanstack/react-router';

import CardRadioGroups from '@/app/components/radio-card';

export const Route = createFileRoute('/components/radio-card/')({
  component: CardRadioGroups,
});
