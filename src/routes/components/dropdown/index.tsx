import { createFileRoute } from '@tanstack/react-router';

import Dropdowns from '@/app/components/dropdown';

export const Route = createFileRoute('/components/dropdown/')({
  component: Dropdowns,
});
