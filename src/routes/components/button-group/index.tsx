import { createFileRoute } from '@tanstack/react-router';

import ButtonGroups from '@/app/components/button-group';

export const Route = createFileRoute('/components/button-group/')({
  component: ButtonGroups,
});
